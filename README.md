# Potentiometer
A potentiometer is a variable resistor that gives an analog value (0 to 1023) to the Arduino.  When the knob is at minimum (0) → output ≈ 0 → LED OFF When the knob is turned slightly (> 0) → output > 0 → LED ON
